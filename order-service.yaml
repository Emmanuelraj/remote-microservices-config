---
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/order-service-dev
  jpa:
    database: POSTGRESQL
    generate-ddl: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
