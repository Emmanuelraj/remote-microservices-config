---
spring:
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/order-service-dev
  jpa:
    database: POSTGRESQL
    generate-ddl: true
    hibernate:
      show-sql: true
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
